"use client";

type Row={date:string;farm:string;field:string;type:string;product?:string;dose?:number|null;doseUnit?:string;quantity?:number|null;quantityUnit?:string;treatedArea?:number|null;machine?:string;operator?:string;weather?:string;notes?:string};
function q(v:unknown){const s=String(v??"").replace(/"/g,'""');return `"${s}"`}
export function OperationExportButton({rows,fileName="agrar-mentor-muveleti-naplo.csv"}:{rows:Row[];fileName?:string}){function download(){const head=["Dátum","Gazdaság","Földtábla","Művelet","Anyag","Dózis","Dózis egység","Mennyiség","Mennyiség egység","Kezelt terület (ha)","Gép","Végrehajtó","Körülmények","Megjegyzés"];const body=rows.map(r=>[r.date,r.farm,r.field,r.type,r.product,r.dose,r.doseUnit,r.quantity,r.quantityUnit,r.treatedArea,r.machine,r.operator,r.weather,r.notes].map(q).join(";"));const csv="\ufeff"+[head.map(q).join(";"),...body].join("\r\n");const blob=new Blob([csv],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=fileName;a.click();URL.revokeObjectURL(url)}return <button type="button" className="btn btn-secondary" onClick={download} disabled={!rows.length}>CSV export ({rows.length})</button>}
