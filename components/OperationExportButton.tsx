"use client";

type Row={date:string;farm:string;field:string;country?:string|null;type:string;subtype?:string|null;product?:string|null;authorizationNumber?:string|null;crop?:string|null;target?:string|null;activeIngredient?:string|null;dose?:number|null;doseUnit?:string|null;quantity?:number|null;quantityUnit?:string|null;treatedArea?:number|null;machine?:string|null;operator?:string|null;weather?:string|null;notes?:string|null;catalogMode?:string|null};
function q(v:unknown){const s=String(v??"").replace(/"/g,'""');return `"${s}"`}
export function OperationExportButton({rows,fileName="agrar-mentor-muveleti-naplo.csv"}:{rows:Row[];fileName?:string}){function download(){const head=["Dátum","Gazdaság","Földtábla","Ország","Művelet","Munkafolyamat","Anyag / készítmény","Engedélyszám","Kultúra","Célkárosító / cél","Hatóanyag","Dózis","Dózis egység","Mennyiség","Mennyiség egység","Kezelt terület (ha)","Gép","Végrehajtó","Körülmények","Megjegyzés","Adatforrás mód"];const body=rows.map(r=>[r.date,r.farm,r.field,r.country,r.type,r.subtype,r.product,r.authorizationNumber,r.crop,r.target,r.activeIngredient,r.dose,r.doseUnit,r.quantity,r.quantityUnit,r.treatedArea,r.machine,r.operator,r.weather,r.notes,r.catalogMode].map(q).join(";"));const csv="\ufeff"+[head.map(q).join(";"),...body].join("\r\n");const blob=new Blob([csv],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=fileName;a.click();URL.revokeObjectURL(url)}return <button type="button" className="btn btn-secondary" onClick={download} disabled={!rows.length}>CSV export ({rows.length})</button>}
