import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import {AdminNav} from "@/components/AdminNav";
import {inviteFarmer,createFarm,createField} from "../actions";
import styles from "./clients.module.css";

export default async function ClientsPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:me}=await supabase.from("profiles").select("role").eq("id",user.id).maybeSingle();if(me?.role!=="advisor")redirect("/dashboard");const[{data:farmers},{data:farms},{data:fields}]=await Promise.all([supabase.from("profiles").select("id,full_name,phone").eq("role","farmer").order("full_name"),supabase.from("farms").select("id,name,settlement,owner_id").order("created_at",{ascending:false}),supabase.from("fields").select("id,name,farm_id,area_ha,current_crop").order("created_at",{ascending:false})]);return <main className={`admin-shell ${styles.scope}`}><header className="admin-header"><div><span className="eyebrow">SZAKTANÁCSADÓI VEZÉRLŐPULT</span><h1>Ügyfelek</h1><p>Gazdálkodók, gazdaságok és földtáblák kezelése.</p></div></header><AdminNav active="clients"/><section className="admin-grid"><article className="panel"><span className="eyebrow">ÚJ ÜGYFÉL</span><h2>Gazdálkodó meghívása</h2><form action={inviteFarmer} className="admin-form"><label>Gazdálkodó neve<input name="full_name" required/></label><label>E-mail cím<input name="email" type="email" required/></label><button className="btn btn-primary">Meghívó küldése</button></form></article><article className="panel"><span className="eyebrow">ÜGYFELEK</span><h2>Gazdálkodók</h2>{farmers?.length?<div className="advisor-client-list">{farmers.map(f=>{const ownFarms=farms?.filter(x=>x.owner_id===f.id)??[];const farmIds=ownFarms.map(x=>x.id);const ownFields=fields?.filter(x=>farmIds.includes(x.farm_id))??[];return <Link href={`/admin/clients/${f.id}`} className="advisor-client-card" key={f.id}><span className="advisor-client-avatar">{(f.full_name||"G").slice(0,2).toUpperCase()}</span><div><strong>{f.full_name||"Névtelen ügyfél"}</strong><small>{f.phone||"Nincs telefonszám"}</small></div><div className="advisor-client-stats"><span>{ownFarms.length} gazdaság</span><span>{ownFields.length} tábla</span></div><b>Megnyitás →</b></Link>})}</div>:<div className="empty-state">Még nincs ügyfél.</div>}</article></section><section className="admin-grid"><article className="panel"><span className="eyebrow">ÚJ GAZDASÁG</span><h2>Gazdaság felvétele</h2><form action={createFarm} className="admin-form"><label>Gazdálkodó<select name="owner_id" required><option value="">Válassz ügyfelet</option>{farmers?.map(f=><option key={f.id} value={f.id}>{f.full_name}</option>)}</select></label><label>Gazdaság neve<input name="name" required/></label><label>Település<input name="settlement"/></label><label>Cím<input name="address"/></label><button className="btn btn-primary">Gazdaság létrehozása</button></form></article><article className="panel"><span className="eyebrow">ÚJ FÖLDTÁBLA</span><h2>Földtábla felvétele</h2><form action={createField} className="admin-form"><label>Gazdaság<select name="farm_id" required><option value="">Válassz gazdaságot</option>{farms?.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label><label>Tábla neve<input name="name" required/></label><label>Terület (ha)<input name="area_ha"/></label><label>Kultúra<input name="current_crop"/></label><button className="btn btn-primary">Tábla létrehozása</button></form></article></section><section className="panel"><span className="eyebrow">NYILVÁNTARTÁS</span><h2>Gazdaságok és táblák</h2>{farms?.map(f=><div className="admin-row" key={f.id}><div><strong>{f.name}</strong><small>{f.settlement||"Nincs település"}</small></div><span>{fields?.filter(x=>x.farm_id===f.id).length??0} tábla</span><span>{fields?.filter(x=>x.farm_id===f.id).reduce((s,x)=>s+Number(x.area_ha||0),0).toLocaleString("hu-HU")} ha</span></div>)}</section></main>}
