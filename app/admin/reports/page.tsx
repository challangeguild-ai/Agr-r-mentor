import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import {AdminNav} from "@/components/AdminNav";
import {BlockHelpButton} from "@/components/GuidedTour";
import {CommunicationDisclosure,RemindLaterButton,SeenStatus} from "@/components/CommunicationControls";
import {handleReport} from "../actions";

type SearchParams=Promise<{view?:string}>;
function reportStatus(v:string){if(v==="reviewed")return"Megválaszolva";if(v==="closed")return"Lezárva";return"Új"}

export default async function ReportsPage({searchParams}:{searchParams:SearchParams}){
 const{view="open"}=await searchParams;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:me}=await supabase.from("profiles").select("role").eq("id",user.id).maybeSingle();if(me?.role!=="advisor")redirect("/dashboard");const[{data:reports},{data:fields},{data:farms},{data:farmers}]=await Promise.all([supabase.from("farmer_reports").select("id,field_id,title,message,status,created_at,advisor_reply,replied_at").order("created_at",{ascending:false}),supabase.from("fields").select("id,name,farm_id"),supabase.from("farms").select("id,name,owner_id"),supabase.from("profiles").select("id,full_name").eq("role","farmer")]);const reportIds=(reports??[]).map(r=>r.id);const{data:receipts}=reportIds.length?await supabase.from("communication_receipts").select("entity_id,viewer_id,first_seen_at,last_seen_at").eq("entity_type","farmer_report").in("entity_id",reportIds):{data:[]};const all=reports??[];const fresh=all.filter(r=>r.status!=="reviewed"&&r.status!=="closed");const answered=all.filter(r=>r.status==="reviewed");const closed=all.filter(r=>r.status==="closed");const visible=all.filter(r=>view==="new"?r.status!=="reviewed"&&r.status!=="closed":view==="answered"?r.status==="reviewed":view==="closed"?r.status==="closed":view==="all"?true:r.status!=="closed");const farmerFor=(fieldId:string)=>{const field=fields?.find(f=>f.id===fieldId);const farm=farms?.find(f=>f.id===field?.farm_id);return{farm,profile:farmers?.find(p=>p.id===farm?.owner_id)}};
 return <main className="admin-shell"><header className="admin-header"><div><span className="eyebrow">SZAKTANÁCSADÓI VEZÉRLŐPULT</span><h1>Bejelentések</h1><p>Gazdálkodói jelzések, válaszok, teendők és lezárások egy munkafolyamatban.</p></div></header><AdminNav active="reports"/><section className="stats-grid"><article className="stat-card"><span>Új</span><strong>{fresh.length}</strong><small>Válaszra vár</small></article><article className="stat-card"><span>Megválaszolva</span><strong>{answered.length}</strong><small>Nyitott ügy</small></article><article className="stat-card"><span>Nyitott összesen</span><strong>{fresh.length+answered.length}</strong><small>Aktív bejelentés</small></article><article className="stat-card"><span>Lezárt</span><strong>{closed.length}</strong><small>Befejezett ügy</small></article></section><section className="panel" data-help-block="advisor-reports"><div className="panel-heading"><div><span className="eyebrow">MUNKALISTA</span><h2>Bejelentések kezelése</h2></div><div style={{display:"flex",gap:8,alignItems:"center"}}><span className="user-pill">{visible.length} találat</span><BlockHelpButton label="A bejelentések kezelésének magyarázata" content={{title:"Gazdálkodói bejelentések kezelése",body:"A gazdálkodó jelzése csak akkor kap „Látta” állapotot, amikor ténylegesen megnyitod a bejegyzést. A listán való átgörgetés önmagában nem számít megtekintésnek.",important:"Az Emlékeztess később privát: a gazdálkodó nem látja, hogy saját utánkövetést állítottál be.",steps:["Nyisd meg a kívánt bejelentést.","Olvasd el a teljes jelzést és ellenőrizd a táblát.","Ha szükséges, állíts privát emlékeztetőt.","Válaszolj, adj ki teendőt vagy zárd le az ügyet."]}}/></div></div><div style={{display:"flex",gap:8,flexWrap:"wrap",marginBottom:18}}><Link className={view==="open"?"btn btn-primary":"ghost-btn"} href="/admin/reports?view=open">Nyitott ({fresh.length+answered.length})</Link><Link className={view==="new"?"btn btn-primary":"ghost-btn"} href="/admin/reports?view=new">Új ({fresh.length})</Link><Link className={view==="answered"?"btn btn-primary":"ghost-btn"} href="/admin/reports?view=answered">Megválaszolva ({answered.length})</Link><Link className={view==="closed"?"btn btn-primary":"ghost-btn"} href="/admin/reports?view=closed">Lezárt ({closed.length})</Link><Link className={view==="all"?"btn btn-primary":"ghost-btn"} href="/admin/reports?view=all">Összes ({all.length})</Link></div>{visible.length?<div className="report-admin-list">{visible.map(r=>{const field=fields?.find(f=>f.id===r.field_id);const farmer=farmerFor(r.field_id);const farmerSeen=(receipts??[]).find(x=>x.entity_id===r.id&&x.viewer_id===farmer.farm?.owner_id)?.last_seen_at;return <div className="report-workflow-card" key={r.id}><CommunicationDisclosure entityType="farmer_report" entityId={r.id} summary={<div style={{display:"flex",gap:10,justifyContent:"space-between",alignItems:"center",width:"100%"}}><div style={{minWidth:0}}><strong style={{display:"block"}}>{r.title}</strong><small>{farmer.profile?.full_name||farmer.farm?.name||"Gazdálkodó"} · {field?.name||"Földtábla"} · {new Date(r.created_at).toLocaleString("hu-HU")}</small></div><span className={`report-status ${r.status}`}>{reportStatus(r.status)}</span></div>}><div>{r.message&&<p>{r.message}</p>}{r.advisor_reply&&<div className="recommendation"><b>Szaktanácsadói válasz</b><span>{r.advisor_reply}</span>{r.replied_at&&<small>{new Date(r.replied_at).toLocaleString("hu-HU")}</small>}<SeenStatus seenAt={farmerSeen} viewerLabel="A gazdálkodó"/></div>}<div className="report-actions"><div style={{display:"flex",gap:8,flexWrap:"wrap",alignItems:"center"}}><Link className="ghost-btn" href={`/fields/${r.field_id}`}>Tábla megnyitása</Link><RemindLaterButton entityType="farmer_report" entityId={r.id} title={r.title} href="/admin/reports"/></div>{r.status!=="closed"&&<form action={handleReport} className="report-reply-form"><input type="hidden" name="report_id" value={r.id}/><label>Válasz a gazdálkodónak<textarea name="reply" rows={3} placeholder="Szakmai válasz vagy javaslat..."/></label><div className="report-task-options"><input name="task_title" placeholder="Teendő neve (opcionális)"/><input name="due_date" type="date"/><select name="priority" defaultValue="normal"><option value="normal">Normál</option><option value="high">Fontos</option><option value="urgent">Sürgős</option></select></div><div className="report-buttons"><button className="btn btn-primary" name="action" value="reply">Válasz küldése</button><button className="btn btn-secondary" name="action" value="reply_task">Válasz + teendő</button><button className="ghost-btn" name="action" value="close" formNoValidate>Lezárás</button></div></form>}</div></div></CommunicationDisclosure></div>})}</div>:<div className="empty-state">Ebben a nézetben nincs bejelentés.</div>}</section></main>
}
