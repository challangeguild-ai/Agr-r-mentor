import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import {AdminNav} from "@/components/AdminNav";
import {handleReport} from "../actions";
function reportStatus(v:string){if(v==="reviewed")return"Megválaszolva";if(v==="closed")return"Lezárva";return"Új"}
export default async function ReportsPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:me}=await supabase.from("profiles").select("role").eq("id",user.id).maybeSingle();if(me?.role!=="advisor")redirect("/dashboard");const[{data:reports},{data:fields}]=await Promise.all([supabase.from("farmer_reports").select("id,field_id,title,message,status,created_at,advisor_reply,replied_at").order("created_at",{ascending:false}),supabase.from("fields").select("id,name")]);return <main className="admin-shell"><header className="admin-header"><div><span className="eyebrow">SZAKTANÁCSADÓI VEZÉRLŐPULT</span><h1>Bejelentések</h1><p>Gazdálkodói jelzések, válaszok és lezárások kezelése.</p></div></header><AdminNav active="reports"/><section className="panel"><span className="eyebrow">GAZDÁLKODÓI BEJELENTÉSEK</span><h2>Bejelentések kezelése</h2>{reports?.length?<div className="report-admin-list">{reports.map(r=>{const field=fields?.find(f=>f.id===r.field_id);return <div className="report-workflow-card" key={r.id}><div className="report-workflow-head"><div><strong>{r.title}</strong><small>{field?.name||"Földtábla"} · {new Date(r.created_at).toLocaleString("hu-HU")}</small></div><span className={`report-status ${r.status}`}>{reportStatus(r.status)}</span></div>{r.message&&<p>{r.message}</p>}{r.advisor_reply&&<div className="recommendation"><b>Szaktanácsadói válasz</b><span>{r.advisor_reply}</span></div>}<div className="report-actions"><Link className="ghost-btn" href={`/fields/${r.field_id}`}>Tábla megnyitása</Link>{r.status!=="closed"&&<form action={handleReport} className="report-reply-form"><input type="hidden" name="report_id" value={r.id}/><label>Válasz a gazdálkodónak<textarea name="reply" rows={3} placeholder="Írd le a szakmai választ vagy javaslatot..."/></label><div className="report-task-options"><input name="task_title" placeholder="Teendő neve (opcionális)"/><input name="due_date" type="date"/><select name="priority" defaultValue="normal"><option value="normal">Normál</option><option value="high">Fontos</option><option value="urgent">Sürgős</option></select></div><div className="report-buttons"><button className="btn btn-primary" name="action" value="reply">Válasz küldése</button><button className="btn btn-secondary" name="action" value="reply_task">Válasz + teendő</button><button className="ghost-btn" name="action" value="close" formNoValidate>Lezárás</button></div></form>}</div></div>})}</div>:<div className="empty-state">Még nincs gazdálkodói bejelentés.</div>}</section></main>}
