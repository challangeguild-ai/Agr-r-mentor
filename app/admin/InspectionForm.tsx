"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {createInspection} from "./inspectionActions";
type FieldOption={id:string;name:string;farmName:string};
export function InspectionForm({fields,defaultFieldId=""}:{fields:FieldOption[];defaultFieldId?:string}){const[busy,setBusy]=useState(false),[error,setError]=useState(""),[createTask,setCreateTask]=useState(false);const supabase=createClient();async function submit(formData:FormData){setBusy(true);setError("");try{const files=formData.getAll("media").filter((v):v is File=>v instanceof File&&v.size>0);formData.delete("media");const inspectionId=await createInspection(formData);for(const file of files){if(file.size>50*1024*1024)throw new Error(`${file.name}: maximum 50 MB lehet.`);const kind=file.type.startsWith("image/")?"image":file.type.startsWith("video/")?"video":null;if(!kind)throw new Error(`${file.name}: csak kép vagy videó tölthető fel.`);const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${inspectionId}/${crypto.randomUUID()}-${safeName}`;const{error:uploadError}=await supabase.storage.from("inspection-media").upload(path,file,{contentType:file.type,upsert:false});if(uploadError)throw uploadError;const{data:{user}}=await supabase.auth.getUser();const{error:rowError}=await supabase.from("inspection_media").insert({inspection_id:inspectionId,storage_path:path,file_name:file.name,media_type:kind,mime_type:file.type,size_bytes:file.size,uploaded_by:user?.id});if(rowError)throw rowError}window.location.reload()}catch(e){setError(e instanceof Error?e.message:"A szemle mentése sikertelen.");setBusy(false)}}return <form action={submit} className="admin-form inspection-create-grid" data-help="inspection-form"><label data-help="inspection-field">Földtábla<select name="field_id" required defaultValue={defaultFieldId}><option value="">Válassz táblát</option>{fields.map(f=><option key={f.id} value={f.id}>{f.name} – {f.farmName}</option>)}</select></label><label>Szemle dátuma<input name="inspected_at" type="date" defaultValue={new Date().toISOString().slice(0,10)}/></label><label>Állapot / minősítés<select name="condition" defaultValue="good" required><option value="good">Jó állapot</option><option value="attention">Figyelmet igényel</option><option value="critical">Kritikus</option></select></label><label>Előző szemléhez képest<select name="follow_up_status" defaultValue=""><option value="">Első szemle / nincs összevetés</option><option value="improved">Javult</option><option value="unchanged">Változatlan</option><option value="worsened">Romlott</option></select></label><label>Következő javasolt ellenőrzés<input name="next_check_at" type="date"/><small>Ha szükséges visszaellenőrzés, itt ütemezhető.</small></label><label>Probléma állapota<select name="issue_status" defaultValue="open"><option value="open">Nyitott</option><option value="monitoring">Megfigyelés alatt</option><option value="resolved">Lezárt / megoldódott</option></select></label><label className="inspection-wide" data-help="inspection-observation">Belső szakmai megfigyelés<textarea name="notes" rows={3} placeholder="Szakmai jegyzet a szemléről. Ez nem kerül automatikusan e-mailben a gazdálkodóhoz."/><small>Belső munkajegyzet; a gazdálkodói e-mailhez használd az alábbi külön mezőt.</small></label><label className="inspection-wide" data-help="inspection-recommendation">Szaktanácsadói javaslat<textarea name="recommendation" rows={3} placeholder="Javasolt kezelés, következő lépés..."/></label><div className="inspection-wide task-from-inspection"><label>Gazdálkodónak szóló észrevétel<textarea name="farmer_message" rows={4} placeholder="Pl. Az állomány megfelelően fejlődik, jelenleg beavatkozás nem szükséges."/></label><label className="checkbox-row"><input type="checkbox" name="notify_farmer" value="yes" defaultChecked/> Értesítés és e-mail küldése a gazdálkodónak</label><small>Ide pozitív és negatív visszajelzés is írható. Ha a mező üres, külön szemle-e-mail nem kerül kiküldésre.</small></div><div className="inspection-wide task-from-inspection" data-help="inspection-task"><label className="checkbox-row"><input type="checkbox" name="create_task" value="yes" checked={createTask} onChange={e=>setCreateTask(e.target.checked)}/> Teendő létrehozása ebből a szemléből</label>{createTask&&<div className="inspection-task-grid"><label>Teendő neve<input name="task_title" placeholder="pl. Gyomirtás" required/></label><label>Határidő<input name="task_due_date" type="date"/></label><label>Prioritás<select name="task_priority" defaultValue="normal"><option value="normal">Normál</option><option value="high">Fontos</option><option value="urgent">Sürgős</option></select></label></div>}</div><label className="inspection-wide" data-help="inspection-media">Képek és videók<input name="media" type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/webm,video/quicktime" multiple/><small>Egyszerre több fájl is választható. Maximum 50 MB / fájl.</small></label>{error&&<div className="error-box inspection-wide">{error}</div>}<button className="btn btn-primary" type="submit" disabled={busy} data-help="inspection-save">{busy?"Mentés és feltöltés…":"Szemle mentése"}</button></form>