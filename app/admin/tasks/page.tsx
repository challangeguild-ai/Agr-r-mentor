import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import {AdminNav} from "@/components/AdminNav";
import {createTask} from "../actions";
function priorityLabel(v:string){if(v==="urgent")return"Sürgős";if(v==="high")return"Fontos";return"Normál"}
export default async function TasksAdminPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:me}=await supabase.from("profiles").select("role").eq("id",user.id).maybeSingle();if(me?.role!=="advisor")redirect("/dashboard");const[{data:farms},{data:fields},{data:tasks}]=await Promise.all([supabase.from("farms").select("id,name").order("name"),supabase.from("fields").select("id,name,farm_id").order("name"),supabase.from("tasks").select("id,title,description,due_date,priority,status,farm_id,field_id,created_at").order("created_at",{ascending:false}).limit(150)]);return <main className="admin-shell"><header className="admin-header"><div><span className="eyebrow">SZAKTANÁCSADÓI VEZÉRLŐPULT</span><h1>Teendők</h1><p>Új feladatok kiadása és a végrehajtás követése.</p></div></header><AdminNav active="tasks"/><section className="panel"><span className="eyebrow">ÚJ TEENDŐ</span><h2>Feladat kiadása</h2><form action={createTask} className="admin-form task-create-grid"><label>Gazdaság<select name="farm_id" required><option value="">Válassz gazdaságot</option>{farms?.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label><label>Földtábla<select name="field_id"><option value="">Teljes gazdaság</option>{fields?.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label><label>Feladat<input name="title" required/></label><label>Határidő<input name="due_date" type="date"/></label><label>Prioritás<select name="priority"><option value="normal">Normál</option><option value="high">Fontos</option><option value="urgent">Sürgős</option></select></label><label className="task-description">Megjegyzés<input name="description"/></label><button className="btn btn-primary">Teendő kiadása</button></form></section><section className="panel"><div className="panel-heading"><div><span className="eyebrow">FELADATLISTA</span><h2>Kiadott teendők</h2></div><span className="user-pill">{tasks?.length??0} teendő</span></div>{tasks?.length?tasks.map(t=><div className="task-admin-row" key={t.id}><span className={`dot ${t.priority}`}/><div><strong>{t.title}</strong><small>{fields?.find(f=>f.id===t.field_id)?.name||farms?.find(f=>f.id===t.farm_id)?.name||"Gazdaság"} · {priorityLabel(t.priority)}</small></div><div><b>{t.due_date?new Date(t.due_date).toLocaleDateString("hu-HU"):"Nincs határidő"}</b><small>{t.status==="done"?"Kész":"Nyitott"}</small></div>{t.field_id&&<Link href={`/fields/${t.field_id}`}>Megnyitás →</Link>}</div>):<div className="empty-state">Még nincs kiadott teendő.</div>}</section></main>}
