import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import "@/app/timeline/timeline.css";

type SearchParams=Promise<{type?:string}>;
function group(t:string|null){if(t==="inspection")return"inspection";if(t==="task"||t==="task_completed")return"task";if(t==="farmer_report"||t==="advisor_reply"||t==="report_closed")return"communication";return"other"}
function label(t:string|null){const m:Record<string,string>={inspection:"Szemle",task:"Teendő kiadva",task_completed:"Teendő elvégezve",farmer_report:"Gazdálkodói bejelentés",advisor_reply:"Szaktanácsadói válasz",report_closed:"Bejelentés lezárva"};return m[t||""]||"Naplóbejegyzés"}
function icon(t:string|null){if(t==="inspection")return"◉";if(t==="task_completed"||t==="report_closed")return"✓";if(t==="task")return"!";if(t==="advisor_reply")return"↩";return"●"}
function clean(v:string){return v.replace("Táblaszemle: attention","Táblaszemle: Figyelmet igényel").replace("Táblaszemle: good","Táblaszemle: Jó állapot").replace("Táblaszemle: critical","Táblaszemle: Kritikus")}
function day(v:string){return new Intl.DateTimeFormat("hu-HU",{timeZone:"Europe/Budapest",year:"numeric",month:"long",day:"numeric",weekday:"long"}).format(new Date(v))}
function key(v:string){return new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Budapest",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(v))}
function time(v:string){return new Intl.DateTimeFormat("hu-HU",{timeZone:"Europe/Budapest",hour:"2-digit",minute:"2-digit"}).format(new Date(v))}

export default async function AdminTimelinePage({searchParams}:{searchParams:SearchParams}){const{type="all"}=await searchParams;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:me}=await supabase.from("profiles").select("role").eq("id",user.id).maybeSingle();if(me?.role!=="advisor")redirect("/dashboard");const{data:events,error}=await supabase.from("timeline_events").select("id,farm_id,field_id,event_type,title,description,event_at,created_at").order("event_at",{ascending:false}).limit(300);if(error)throw new Error(error.message);const all=events??[];const fieldIds=[...new Set(all.map(e=>e.field_id).filter(Boolean))] as string[];const farmIds=[...new Set(all.map(e=>e.farm_id).filter(Boolean))] as string[];const[{data:fields},{data:farms}]=await Promise.all([fieldIds.length?supabase.from("fields").select("id,name").in("id",fieldIds):Promise.resolve({data:[]}),farmIds.length?supabase.from("farms").select("id,name").in("id",farmIds):Promise.resolve({data:[]})]);const counts={all:all.length,inspection:all.filter(e=>group(e.event_type)==="inspection").length,task:all.filter(e=>group(e.event_type)==="task").length,communication:all.filter(e=>group(e.event_type)==="communication").length};const visible=all.filter(e=>type==="all"||group(e.event_type)===type);const grouped=visible.reduce<Record<string,typeof all>>((a,e)=>{const v=e.event_at||e.created_at,k=key(v);(a[k]??=[]).push(e);return a},{});return <main className="admin-shell"><header className="admin-header"><div><span className="eyebrow">TELJES ÜGYFÉLKÖR</span><h1>Idővonal</h1><p>Minden ügyfél és földtábla fontos eseménye időrendben.</p></div></header><section className="admin-overview-grid timeline-stats"><article className="admin-overview-card"><span>Összes esemény</span><strong>{counts.all}</strong><small>Legutóbbi 300</small></article><article className="admin-overview-card"><span>Szemlék</span><strong>{counts.inspection}</strong><small>Helyszíni ellenőrzések</small></article><article className="admin-overview-card"><span>Teendők</span><strong>{counts.task}</strong><small>Kiadott és elvégzett</small></article><article className="admin-overview-card"><span>Kapcsolattartás</span><strong>{counts.communication}</strong><small>Bejelentések és válaszok</small></article></section><section className="panel timeline-page-panel"><div className="timeline-filter-tabs"><Link className={type==="all"?"active":""} href="/admin/timeline?type=all">Minden <span>{counts.all}</span></Link><Link className={type==="inspection"?"active":""} href="/admin/timeline?type=inspection">Szemlék <span>{counts.inspection}</span></Link><Link className={type==="task"?"active":""} href="/admin/timeline?type=task">Teendők <span>{counts.task}</span></Link><Link className={type==="communication"?"active":""} href="/admin/timeline?type=communication">Kapcsolattartás <span>{counts.communication}</span></Link></div>{visible.length?Object.entries(grouped).map(([k,items])=><section className="timeline-day-group" key={k}><div className="timeline-day-heading"><span className="calendar-icon">▣</span><span>{day(items[0].event_at||items[0].created_at)}</span></div><div className="timeline-list global-timeline-clean">{items.map(e=>{const v=e.event_at||e.created_at,g=group(e.event_type),field=fields?.find(f=>f.id===e.field_id),farm=farms?.find(f=>f.id===e.farm_id);return <article className="timeline-item" key={e.id}><span className={`timeline-event-icon ${g}`}>{icon(e.event_type)}</span><div className="timeline-event-card"><div className="timeline-event-top"><span className={`timeline-type-pill ${g}`}>{label(e.event_type)}</span><time>{time(v)}</time></div><strong className="timeline-event-title">{clean(e.title)}</strong><small className="timeline-location">{field?.name||farm?.name||"Gazdasági esemény"}</small>{e.description&&<p>{e.description}</p>}{e.field_id&&<Link className="timeline-field-link" href={`/fields/${e.field_id}`}>Földtábla megnyitása →</Link>}</div></article>})}</div></section>):<div className="empty-state">Ebben a kategóriában még nincs esemény.</div>}</section></main>}
