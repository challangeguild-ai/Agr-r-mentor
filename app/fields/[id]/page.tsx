import Link from "next/link";
import { notFound,redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { Sidebar } from "@/components/Sidebar";
import { InspectionMedia } from "@/components/InspectionMedia";
function formatDate(v:string|null|undefined){return v?new Date(v).toLocaleDateString("hu-HU"):"—"}
function conditionLabel(v:string|null|undefined){if(v==="good")return "Jó állapot";if(v==="attention")return "Figyelmet igényel";if(v==="critical")return "Kritikus";return v||"Táblaszemle"}
function eventTypeLabel(v:string|null|undefined){if(v==="inspection")return "Szemle";if(v==="task")return "Teendő";return v||"Napló"}
export default async function FieldDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const[{data:profile},{data:field}]=await Promise.all([supabase.from("profiles").select("role").eq("id",user.id).maybeSingle(),supabase.from("fields").select("id,farm_id,name,area_ha,current_crop,crop_year,sowing_date,status,notes").eq("id",id).maybeSingle()]);if(!field)notFound();const[{data:farm},{data:tasks},{data:inspections},{data:timeline}]=await Promise.all([supabase.from("farms").select("name,settlement,address").eq("id",field.farm_id).maybeSingle(),supabase.from("tasks").select("id,title,description,due_date,priority,status,created_at").eq("field_id",id).order("created_at",{ascending:false}),supabase.from("inspections").select("id,inspected_at,condition,notes,recommendation,created_at").eq("field_id",id).order("inspected_at",{ascending:false}),supabase.from("timeline_events").select("id,event_type,title,description,event_at,created_at").eq("field_id",id).order("event_at",{ascending:false})]);const inspectionIds=(inspections??[]).map(i=>i.id);const{data:media}=inspectionIds.length?await supabase.from("inspection_media").select("id,inspection_id,storage_path,file_name,media_type").in("inspection_id",inspectionIds).order("created_at"):{data:[]};const events=[...(tasks??[]).map(x=>({id:`t-${x.id}`,type:"Teendő",title:x.title,description:x.description||"Kiadott feladat.",date:x.created_at})),...(timeline??[]).map(x=>({id:`e-${x.id}`,type:eventTypeLabel(x.event_type),title:x.event_type==="inspection"?x.title.replace("Táblaszemle: good","Táblaszemle: Jó állapot").replace("Táblaszemle: attention","Táblaszemle: Figyelmet igényel").replace("Táblaszemle: critical","Táblaszemle: Kritikus"):x.title,description:x.description||"",date:x.event_at||x.created_at}))].sort((a,b)=>new Date(b.date).getTime()-new Date(a.date).getTime());return <div className="app-shell"><Sidebar/><main className="dashboard"><header className="field-detail-header"><div><Link href="/dashboard" className="back-link">← Vissza az áttekintéshez</Link><span className="eyebrow">FÖLDTÁBLA ADATLAP</span><h1>{field.name}</h1><p>{farm?.name||"Gazdaság"}{farm?.settlement?` · ${farm.settlement}`:""}</p></div><div className="topbar-actions">{profile?.role==="advisor"&&<Link className="btn btn-primary" href="/admin">Szaktanácsadói admin</Link>}<span className="user-pill">{profile?.role==="advisor"?"Szaktanácsadó":"Gazdálkodó"}</span></div></header><section className="field-detail-stats"><article className="stat-card"><span>Terület</span><strong>{field.area_ha?`${field.area_ha} ha`:"—"}</strong><small>Nyilvántartott terület</small></article><article className="stat-card"><span>Aktuális kultúra</span><strong className="field-stat-text">{field.current_crop||"—"}</strong><small>{field.crop_year||new Date().getFullYear()}. gazdasági év</small></article><article className="stat-card"><span>Vetés</span><strong className="field-stat-text">{formatDate(field.sowing_date)}</strong><small>Vetési dátum</small></article><article className="stat-card"><span>Nyitott teendők</span><strong>{(tasks??[]).filter(t=>t.status!=="done").length}</strong><small>Ehhez a táblához</small></article></section><section className="field-detail-grid"><article className="panel"><div className="panel-heading"><div><span className="eyebrow">SZAKTANÁCSADÁS</span><h2>Szemlék és javaslatok</h2></div><span className="field-total">{inspections?.length??0} szemle</span></div>{inspections?.length?<div className="inspection-list">{inspections.map(i=><div className="inspection-card" key={i.id}><div className="inspection-head"><div><strong>{conditionLabel(i.condition)}</strong><small>{formatDate(i.inspected_at)}</small></div><span className="event-badge">Szemle</span></div>{i.notes&&<p>{i.notes}</p>}{i.recommendation&&<div className="recommendation"><b>Szaktanácsadói javaslat</b><span>{i.recommendation}</span></div>}<InspectionMedia items={(media??[]).filter(m=>m.inspection_id===i.id) as any}/></div>)}</div>:<div className="empty-state">Ehhez a táblához még nincs rögzített szemle.</div>}</article><article className="panel"><div className="panel-heading"><div><span className="eyebrow">FELADATOK</span><h2>Teendők</h2></div></div>{tasks?.length?<div className="task-list">{tasks.map(t=><div className="task-row" key={t.id}><span className={`dot ${t.priority}`}/><div><strong>{t.title}</strong><small>{t.due_date?`Határidő: ${formatDate(t.due_date)}`:"Nincs határidő"}{t.description?` · ${t.description}`:""}</small></div><span className="task-status">{t.status==="done"?"Kész":"Nyitott"}</span></div>)}</div>:<div className="empty-state">Nincs ehhez a táblához kiadott teendő.</div>}</article></section><section className="panel field-timeline-panel"><div className="panel-heading"><div><span className="eyebrow">NAPLÓ</span><h2>Tábla idővonala</h2></div></div>{events.length?<div className="timeline-list">{events.map(e=><div className="timeline-item" key={e.id}><span className="timeline-dot"/><div className="timeline-content"><div className="timeline-meta"><span className="event-badge">{e.type}</span><time>{formatDate(e.date)}</time></div><strong>{e.title}</strong>{e.description&&<p>{e.description}</p>}</div></div>)}</div>:<div className="empty-state">A tábla idővonala még üres.</div>}</section></main></div>}
